REVISE (reflection gate): blocking smell "reflect-spec-defect": reflection gate: acs/AC3-par-level-not-tracked.json is malformed JSON (line 9 has a stray '}: {' merging the object close with the 'independence' key, so the file does not parse, e.g. `jq .` fails with 'Expected string key before ':' at line 9'). Fix the JSON structure so 'independence' is a proper sibling key of 'architectural_notes' inside one well-formed object.

Re-author this story's acceptance criteria to ADD the specific coverage named above. This coverage is REQUIRED: add the missing test(s)/criterion that assert the exact behavior described , do NOT omit it, weaken it, or defer it to an open question. If the stated behavior genuinely cannot be tested as written, name the concrete blocker; do not punt.