# Test list: F1-stock-visibility
Ordered for: design-momentum

- [x] T1: Navigating to the record stock page renders a form with input fields for SKU, location, quantity, and tracking code  (AC1-form-displays)
- [x] T2: Submitting the form with a required field left blank shows an inline validation error that names the missing field  (AC1-form-displays)
- [x] T3: Submitting the form with all valid inputs creates a stock record in the database with the submitted SKU, location, quantity, and tracking code  (AC2-record-created-on-submission)
- [x] T4: After a successful form submission the user sees a confirmation message indicating the stock record was saved  (AC3-confirmation-shown)
- [ ] T5: A tracking code submitted on the form is retrievable from the API without loss or truncation  (AC5-tracking-code-persisted)
- [x] T6: Submitting the form for a SKU and location that already has a record updates the existing record's quantity and shows no error message  (AC4-collision-handled-via-update)
- [x] T7: Submitting a stock record with a negative quantity is rejected at write time and no row with a negative quantity is stored  (AC2-record-created-on-submission)
- [x] T8: The routes/boundary module does not import the DB session; persistence is only reachable through the repository layer (layering contract)  (AC2-record-created-on-submission)
- [ ] T9: The application reads DB_NAME/PGDATABASE from the environment and contains no hardcoded DSN string (config-in-env, NFR-F1-5)  (AC5-tracking-code-persisted)
- [x] T10: The stock table carries a database-level unique constraint on (sku, location) so the upsert strategy is enforced at the schema layer (NFR-F1-3)  (AC4-collision-handled-via-update)
- [x] T11: The pytest-bdd conftest binds to the real paired-branch DB (DB_NAME=stockflow); no mock or in-memory DB substitution is used in any integration test (NFR-F1-4)  (AC2-record-created-on-submission)
- [ ] T12: Navigating to the stock home page when records exist renders a table with one row per seeded stock record  (AC1-table-displays)
- [ ] T13: Navigating to the stock home page when no records exist shows an explicit empty-state guidance message instead of a blank or empty table  (AC5-empty-state)
- [ ] T14: When records span multiple locations the table rows are grouped so all rows for one location appear together before rows for the next location  (AC2-organized-by-location)
- [ ] T15: Each table row displays the SKU, location, quantity, and inventory_code values from the corresponding seeded stock record  (AC3-columns-display)
- [ ] T16: A stock record whose inventory_code is null renders the literal text 'not tracked' in the inventory_code column rather than an empty cell (NFR-F1-6)  (AC3-columns-display)
- [ ] T17: The rendered quantity cell carries a right-align CSS style so the numeric column is visually scannable  (AC4-quantities-right-aligned)
- [ ] T18: The home route module does not import the DB session; the read path reaches the database only through the repository layer (layering contract)  (AC1-table-displays)
- [ ] T19: The repository or service module supplies rows already ordered by location then SKU; no sorting or grouping logic appears in the boundary or template module  (AC2-organized-by-location)
- [ ] T20: Navigating to the detail page for a SKU that has stock at multiple locations renders one row per location with the location name and current quantity  (AC1-detail-view-displays-sku-distribution)
- [ ] T21: Each location row in the SKU detail view displays the inventory tracking code stored on that location's stock record  (AC2-tracking-code-displayed)
- [ ] T22: A location row whose optional fields (e.g., par level) have no stored value displays the literal text 'not tracked' rather than a blank cell or raw null  (AC3-optional-fields-show-not-tracked)
- [ ] T23: The SKU detail route module does not import the DB session; the read path is enforced as boundary -> service -> repository with no ORM access outside the repository (layering contract, NFR-F1-7)  (AC1-detail-view-displays-sku-distribution)
- [ ] T24: Seeding stock for a SKU at N locations and requesting the detail view returns exactly N rows with matching location, quantity, and tracking code for each seeded record, with no records omitted (read completeness, NFR-F1-7)  (AC1-detail-view-displays-sku-distribution)

## Deferred / skipped
- (none)
