{
  "id": "AC6-migration-reverses",
  "given": "the stock schema after the split migration has run, with batch_number and serial_number populated for conforming rows",
  "when": "the migration's down path is run",
  "then": "the combined inventory_code is restored on each row, reconstructed from the canonical location plus its batch_number and serial_number",
  "status": "draft",
  "layer": "Infra",
  "architectural_notes": "Infra: the reversibility contract (PI1 migration_reversible) defending R1. The revision's down() reconstructs the combined inventory_code on each row from the canonical location plus batch_number and serial_number, re-adds the inventory_code column, and drops the split columns. Owner module: the split Alembic revision's down path (mirrors the up path). Verified by a real-branch up/down round-trip that asserts inventory_code is restored to its pre-migration value for conforming rows.",
  "independence": {
    "distinct_from_prior": true,
    "rationale": "Prior ACs assert the forward migration's results; this AC adds the distinct reversibility outcome that the down path reconstructs the combined code, which can be broken (no down path, or a mis-reconstructed code) without affecting any forward-migration outcome."
  }
}
