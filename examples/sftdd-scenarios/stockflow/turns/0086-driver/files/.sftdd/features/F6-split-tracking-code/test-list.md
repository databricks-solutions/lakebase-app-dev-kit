# Test list: F6-split-tracking-code
Ordered for: design-momentum

- [ ] T1: after the up() migration runs, the stock table has a batch_number column  (AC1-columns-added)
- [ ] T2: after the up() migration runs, the stock table has a serial_number column  (AC1-columns-added)
- [ ] T3: a stock row with inventory_code 'A12-B7-S001' has batch_number='B7' and serial_number='S001' after the backfill  (AC2-parse-three-segment-code)
- [ ] T4: a stock row with inventory_code 'A12-B7' has batch_number='B7' and serial_number=NULL after the backfill  (AC3-parse-two-segment-code)
- [ ] T5: a stock row with inventory_code 'A12' has batch_number=NULL and serial_number=NULL after the backfill  (AC4-parse-one-segment-code)
- [ ] T6: every row in a 100-row stock table has batch_number and serial_number present (populated or NULL) after the migration, with no rows skipped  (AC5-migrate-all-rows)
- [ ] T7: the location column value is unchanged for a row after the migration runs  (AC6-preserve-location)
- [ ] T8: sku and quantity are unchanged for a row after the migration runs  (AC7-preserve-business-data)
- [ ] T9: the Alembic revision module does not import app/routes or app/services; persistence logic stays in the repository/migration layer  (AC1-columns-added)
- [ ] T10: the app and Alembic env read DB_NAME/PGDATABASE from the environment; no hardcoded DSN exists anywhere in the migration or config path  (AC1-columns-added)
- [ ] T11: after up() then down(), the original inventory_code value is reconstructed for every row and the batch_number/serial_number columns are removed  (AC1-columns-added)
- [ ] T12: the UNIQUE(sku, location) constraint exists on the stock table after the migration and rejects a duplicate (sku, location) pair  (AC6-preserve-location)
- [ ] T13: quantity values are identical for every row before and after the migration; no negative quantity is introduced  (AC7-preserve-business-data)
- [ ] T14: the integration test suite connects to the real Lakebase branch DB (DB_NAME=stockflow) and not an in-memory substitute; the conftest fixture verifies the live connection before any scenario runs  (AC1-columns-added)
- [ ] T15: the validation probe function can be called against the migrated stock table and returns a result without raising an error  (AC1-verification-available)
- [ ] T16: when every stock row has a three-segment inventory_code the validation probe returns a count of 0  (AC2-verification-zero-when-valid)
- [ ] T17: when the stock table contains rows with non-conforming codes (e.g. 'A12' and 'c') the probe returns a count equal to the number of rows where batch_number IS NULL OR serial_number IS NULL  (AC3-verification-counts-unparseable)
- [ ] T18: a database administrator executing the validation query receives a scalar integer count that can be read and recorded before the drop proceeds  (AC4-verification-accessible)
- [ ] T19: the validation probe module (app/repositories) does not import app/routes or app/services; the probe is never re-implemented outside the repository layer  (AC1-verification-available)
- [ ] T20: the S2 validation test suite binds to the real Lakebase branch DB (DB_NAME=stockflow); no mock or in-memory substitute is used and the conftest fixture verifies the live connection before any probe scenario runs  (AC2-verification-zero-when-valid)
- [x] T21: after the S3 up() migration runs, the stock table has no inventory_code column  (AC1-column-dropped)
- [ ] T22: after the S3 down() migration runs, the inventory_code column is present on the stock table  (AC2-column-restored)
- [ ] T23: after the S3 down() migration runs, every row has a non-NULL inventory_code value  (AC3-all-rows-reconstructed)
- [ ] T24: a row where batch_number is NULL has its inventory_code reconstructed with an empty string in the batch segment position (e.g., 'LOC--S001')  (AC4-format-and-null-handling)
- [ ] T25: a row where serial_number is NULL has its inventory_code reconstructed with an empty string in the serial segment position (e.g., 'LOC-B7-')  (AC4-format-and-null-handling)
- [ ] T26: after the S3 down() migration runs, the batch_number column is STILL PRESENT (preserved; owned by the S1 split migration, not retired by S3.down)  (AC5-split-columns-dropped)
- [ ] T27: after the S3 down() migration runs, the serial_number column is STILL PRESENT (preserved; owned by the S1 split migration, not retired by S3.down)  (AC5-split-columns-dropped)
- [ ] T28: after the S3 down() migration runs, the inventory_code column carries a NOT NULL constraint and an attempt to insert a NULL value is rejected  (AC6-not-null-constraint)
- [x] T29: the S3 Alembic revision module does not import app/routes or app/services; the migration stays in the repository/migration layer and never crosses the layering contract  (AC1-column-dropped)
- [x] T30: the S3 integration test suite binds to the real Lakebase branch DB (DB_NAME=stockflow); no mock or in-memory substitute is used and the conftest fixture verifies the live connection before any S3 scenario runs  (AC1-column-dropped)
- [ ] T31: the home stock table renders a 'Batch Number' column and a 'Serial Number' column when stock records with populated batch_number and serial_number exist  (AC1-table-displays-batch-serial)
- [ ] T32: the SKU detail view renders batch number and serial number as separate labeled fields for each location row when the stock record has batch_number and serial_number populated  (AC3-detail-displays-batch-serial)
- [ ] T33: a stock row with a NULL batch_number renders 'not tracked' in the Batch Number column of the home stock table  (AC2-table-null-renders-not-tracked)
- [ ] T34: a stock row with a NULL serial_number renders 'not tracked' in the Serial Number column of the home stock table  (AC2-table-null-renders-not-tracked)
- [ ] T35: a stock record with a NULL batch_number at a location renders 'not tracked' in the batch number field of the SKU detail view  (AC4-detail-null-renders-not-tracked)
- [ ] T36: a stock record with a NULL serial_number at a location renders 'not tracked' in the serial number field of the SKU detail view  (AC4-detail-null-renders-not-tracked)
- [ ] T37: the stock-list route (app/routes/) does not import app/repositories or the DB session; batch_number and serial_number are projected at the route boundary with no column computation inside the component  (AC1-table-displays-batch-serial)
- [ ] T38: the S4 E2E test suite binds to the real Lakebase branch DB (DB_NAME=stockflow); no mock or in-memory substitute is used and the conftest fixture verifies the live connection before any S4 scenario runs  (AC2-table-null-renders-not-tracked)

## Deferred / skipped
- (none)
