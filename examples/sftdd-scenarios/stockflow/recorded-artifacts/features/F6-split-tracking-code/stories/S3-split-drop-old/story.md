# S3: Remove the inventory_code column and support rollback

As a database administrator
I want to remove the inventory_code column after the backfill and validation are complete
So that the schema reflects the new structure and the combined code is fully retired
