# S2: Validate migration and surface count of unparseable codes

As a database administrator
I want to verify that all inventory codes parse correctly as location-batch-serial and report any that don't
So that the team can review the migration integrity before the old column is dropped
